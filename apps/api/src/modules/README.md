# Domain modules

Reserve this directory for future feature modules (`auth`, `organizations`, `branches`, `users`, `roles`, `permissions`).

Each domain should be added as an isolated Nest module and imported into `app.module.ts` only when that domain lands.
