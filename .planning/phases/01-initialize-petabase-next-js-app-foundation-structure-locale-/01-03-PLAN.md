---
phase: 01-initialize-petabase-next-js-app-foundation-structure-locale
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/petabase/src/lib/navigation/locale-path.ts
  - apps/petabase/src/hooks/use-locale-path.ts
  - apps/petabase/src/lib/utils/classnames.ts
  - apps/petabase/src/lib/api/client.ts
  - docs/petabase-foundation.md
autonomous: true
requirements: [PB-04]
must_haves:
  truths:
    - "Locale-aware link generation is reusable from both server and client helpers."
    - "Core utility helpers exist for class composition and baseline API client usage."
    - "Contributors can follow one canonical doc to extend petabase structure safely."
  artifacts:
    - path: "apps/petabase/src/lib/navigation/locale-path.ts"
      provides: "Canonical locale-prefixed path builder"
    - path: "apps/petabase/src/lib/api/client.ts"
      provides: "Typed API client foundation tied to validated env"
    - path: "docs/petabase-foundation.md"
      provides: "Canonical petabase structure + locale + env + shell conventions"
  key_links:
    - from: "apps/petabase/src/hooks/use-locale-path.ts"
      to: "apps/petabase/src/lib/navigation/locale-path.ts"
      via: "hook wrapper around locale path builder"
      pattern: "withLocale"
    - from: "apps/petabase/src/lib/api/client.ts"
      to: "apps/petabase/src/lib/env.ts"
      via: "base URL selection"
      pattern: "NEXT_PUBLIC_API_BASE_URL|env"
---

<objective>
Finalize shared common/util building blocks and canonical phase documentation for petabase foundation so future feature work can extend without duplicating helpers.

Purpose: Preserve MVP simplicity while preventing utility drift in later phases.
Output: Locale path + utility/client helpers and updated petabase foundation doc.
</objective>

<execution_context>
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
@apps/petabase/src/lib/navigation/locale-path.ts
@apps/petabase/src/hooks/use-locale-path.ts
@apps/petabase/src/lib/utils/classnames.ts
@apps/petabase/src/lib/api/client.ts
@apps/petabase/src/lib/env.ts
@docs/petabase-foundation.md

<interfaces>
From `apps/petabase/src/lib/navigation/locale-path.ts`:
```ts
export function withLocale(locale: AppLocale, path: string): string;
```

From `apps/petabase/src/lib/utils/classnames.ts`:
```ts
export function classNames(...values: Array<string | false | null | undefined>): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate locale-path and utility helpers for shared use</name>
  <files>apps/petabase/src/lib/navigation/locale-path.ts, apps/petabase/src/hooks/use-locale-path.ts, apps/petabase/src/lib/utils/classnames.ts</files>
  <read_first>apps/petabase/src/lib/navigation/locale-path.ts, apps/petabase/src/hooks/use-locale-path.ts, apps/petabase/src/lib/utils/classnames.ts, apps/petabase/src/lib/i18n/config.ts</read_first>
  <action>Keep one canonical path builder (`withLocale`) and ensure the hook wrapper delegates directly to it instead of re-implementing path prefix logic. Keep `classNames` as the base string composition utility with deterministic filtering/joining behavior for component usage. Do not introduce duplicate locale utility functions in route/components.</action>
  <verify>
    <automated>pnpm exec nx typecheck petabase</automated>
  </verify>
  <acceptance_criteria>
    - `use-locale-path.ts` uses the shared `withLocale` helper.
    - locale path helper normalizes leading slash behavior.
    - `classnames.ts` exports a pure utility with no framework coupling.
  </acceptance_criteria>
  <done>Common util layer is single-source and reusable by all feature slices.</done>
</task>

<task type="auto">
  <name>Task 2: Maintain minimal typed API client baseline and document extension rules</name>
  <files>apps/petabase/src/lib/api/client.ts, docs/petabase-foundation.md</files>
  <read_first>apps/petabase/src/lib/api/client.ts, apps/petabase/src/lib/env.ts, docs/petabase-foundation.md, docs/monorepo-foundation.md</read_first>
  <action>Keep `client.ts` as a lightweight typed fetch baseline sourced from validated env base URL (no speculative SDK complexity). Update `docs/petabase-foundation.md` with explicit conventions for route groups, locale strategy, env validation keys, common/util ownership (`src/lib`, `src/hooks`), and extension guidance that keeps internal/admin flows under `petabase` per AGENTS.md.
  </action>
  <verify>
    <automated>pnpm exec nx lint petabase && pnpm exec nx build petabase</automated>
  </verify>
  <acceptance_criteria>
    - `client.ts` derives base URL from env contract and exports reusable request helper(s).
    - `petabase-foundation.md` explicitly lists locale defaults (`th`, `en`) and route-group structure.
    - docs include command list for `serve/build/lint/typecheck` at petabase project level.
  </acceptance_criteria>
  <done>Utility/client layer and canonical docs are aligned and executable for future contributors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI helper input → URL builder | Untrusted path strings flow into route-generation helper |
| env/API config → client request layer | Untrusted config shapes outbound requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | T | locale path helper | mitigate | Normalize path format and enforce locale type instead of raw string concatenation |
| T-01-09 | I | API client defaults | mitigate | Source base URL only from validated env contract; avoid logging sensitive headers/tokens |
| T-01-10 | R | conventions drift | mitigate | Keep docs canonical with explicit file ownership and commands to verify behavior |
</threat_model>

<verification>
- `pnpm exec nx lint petabase`
- `pnpm exec nx typecheck petabase`
- `pnpm exec nx build petabase`
</verification>

<success_criteria>
- Shared locale/util helpers are single-source and imported by consumers.
- API client baseline remains minimal, typed, and env-backed.
- `PB-04` is fully implemented in code + docs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-initialize-petabase-next-js-app-foundation-structure-locale-/01-03-SUMMARY.md`
</output>
