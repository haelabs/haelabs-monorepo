---
phase: 01-initialize-petabase-next-js-app-foundation-structure-locale
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/petabase/src/app/[locale]/layout.tsx
  - apps/petabase/src/app/[locale]/(auth)/layout.tsx
  - apps/petabase/src/app/[locale]/(dashboard)/layout.tsx
  - apps/petabase/src/components/shell/app-shell.tsx
  - apps/petabase/src/components/shell/app-header.tsx
  - apps/petabase/src/components/shell/app-sidebar.tsx
autonomous: true
requirements: [PB-03]
must_haves:
  truths:
    - "Auth pages render without dashboard shell chrome."
    - "Dashboard/admin pages render with a shared locale-aware shell."
    - "Localized text labels render from message catalogs (no hardcoded locale forks in layout files)."
  artifacts:
    - path: "apps/petabase/src/app/[locale]/(auth)/layout.tsx"
      provides: "Auth-only layout boundary"
    - path: "apps/petabase/src/app/[locale]/(dashboard)/layout.tsx"
      provides: "Dashboard shell layout boundary"
    - path: "apps/petabase/src/components/shell/app-shell.tsx"
      provides: "Shared shell composition (header/sidebar/content)"
  key_links:
    - from: "app/[locale]/(dashboard)/layout.tsx"
      to: "components/shell/app-shell.tsx"
      via: "layout composition import"
      pattern: "AppShell"
    - from: "app/[locale]/layout.tsx"
      to: "src/messages/*.ts"
      via: "dictionary loading"
      pattern: "getDictionary|messages"
---

<objective>
Implement the route-group + shell composition pattern for `apps/petabase` so auth, dashboard, and admin foundations are structurally separated from day one.

Purpose: Prevent future rework by locking route ownership and shared shell behavior now.
Output: Locale layout + route-group layouts + shell primitives wired to message catalogs.
</objective>

<execution_context>
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@AGENTS.md
@apps/petabase/src/app/[locale]/layout.tsx
@apps/petabase/src/app/[locale]/(auth)/layout.tsx
@apps/petabase/src/app/[locale]/(dashboard)/layout.tsx
@apps/petabase/src/components/shell/app-shell.tsx
@apps/petabase/src/components/shell/app-header.tsx
@apps/petabase/src/components/shell/app-sidebar.tsx

<interfaces>
From `apps/petabase/src/lib/i18n/config.ts` (Plan 01 output):
```ts
export type AppLocale = 'th' | 'en';
```

From `apps/petabase/src/types/i18n.ts`:
```ts
export type MessageCatalog = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce route-group layout boundaries for auth vs dashboard/admin</name>
  <files>apps/petabase/src/app/[locale]/layout.tsx, apps/petabase/src/app/[locale]/(auth)/layout.tsx, apps/petabase/src/app/[locale]/(dashboard)/layout.tsx</files>
  <read_first>apps/petabase/src/app/[locale]/layout.tsx, apps/petabase/src/app/[locale]/(auth)/layout.tsx, apps/petabase/src/app/[locale]/(dashboard)/layout.tsx, apps/petabase/src/lib/i18n/dictionaries.ts</read_first>
  <action>Keep the parent locale layout responsible for locale parameter validation and dictionary loading; keep `(auth)` layout shell-free for focused sign-in flows; keep `(dashboard)` layout shell-enabled for internal pages. Ensure all layouts use `AppLocale`-safe params and propagate localized messages to children/shell consumers without ad hoc locale branching.</action>
  <verify>
    <automated>pnpm exec nx typecheck petabase</automated>
  </verify>
  <acceptance_criteria>
    - `(auth)/layout.tsx` renders children without `AppShell` import.
    - `(dashboard)/layout.tsx` composes children via `AppShell`.
    - locale layout performs locale-aware dictionary access and compiles without `any`.
  </acceptance_criteria>
  <done>Route-group boundaries are explicit and future domain pages can attach without layout rewrites.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shell components to locale-aware message and navigation primitives</name>
  <files>apps/petabase/src/components/shell/app-shell.tsx, apps/petabase/src/components/shell/app-header.tsx, apps/petabase/src/components/shell/app-sidebar.tsx</files>
  <read_first>apps/petabase/src/components/shell/app-shell.tsx, apps/petabase/src/components/shell/app-header.tsx, apps/petabase/src/components/shell/app-sidebar.tsx, apps/petabase/src/lib/navigation/locale-path.ts</read_first>
  <action>Ensure shell components accept `locale` + `messages` inputs and use locale-aware path helpers for navigation links. Keep header/sidebar copy sourced from message catalog fields (no duplicated static text literals). Preserve admin flow inside `petabase` routes per repository decision (no separate admin app split).</action>
  <verify>
    <automated>pnpm exec nx lint petabase && pnpm exec nx build petabase</automated>
  </verify>
  <acceptance_criteria>
    - Shell component props include locale and message catalog contracts.
    - Sidebar links resolve through locale path helper.
    - `nx build petabase` completes successfully.
  </acceptance_criteria>
  <done>Shell composition is reusable and locale-aware across protected route groups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route params → locale layout | Untrusted URL segment controls content branch |
| message catalog → UI render | Dynamic dictionary keys flow into visible UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | locale route param handling | mitigate | Validate locale via canonical helpers before dictionary resolution |
| T-01-06 | I | shell rendering | accept | This phase renders non-sensitive foundation copy only; no PII paths |
| T-01-07 | E | route ownership boundary | mitigate | Keep admin/dashboard routes under shell-protected group and no standalone `apps/admin` |
</threat_model>

<verification>
- `pnpm exec nx lint petabase`
- `pnpm exec nx typecheck petabase`
- `pnpm exec nx build petabase`
</verification>

<success_criteria>
- Auth and dashboard/admin route groups are structurally separated and stable.
- Shell components are locale-aware and rely on catalog inputs.
- `PB-03` is fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-initialize-petabase-next-js-app-foundation-structure-locale-/01-02-SUMMARY.md`
</output>
