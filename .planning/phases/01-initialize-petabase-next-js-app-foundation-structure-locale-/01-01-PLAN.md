---
phase: 01-initialize-petabase-next-js-app-foundation-structure-locale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/petabase/src/types/i18n.ts
  - apps/petabase/src/lib/i18n/config.ts
  - apps/petabase/src/lib/i18n/locale.ts
  - apps/petabase/src/lib/env.ts
  - apps/petabase/middleware.ts
autonomous: true
requirements: [PB-01, PB-02]
must_haves:
  truths:
    - "Users are always routed under a supported locale prefix (`/th` or `/en`)."
    - "Locale preference persists via cookie and falls back deterministically when absent."
    - "Petabase startup fails fast when required public env values are invalid."
  artifacts:
    - path: "apps/petabase/src/lib/i18n/config.ts"
      provides: "Canonical locale constants, locale type, and normalization helpers"
    - path: "apps/petabase/middleware.ts"
      provides: "Locale negotiation/redirect and optional coarse auth guard routing"
    - path: "apps/petabase/src/lib/env.ts"
      provides: "Centralized zod-validated env contract"
  key_links:
    - from: "apps/petabase/middleware.ts"
      to: "apps/petabase/src/lib/i18n/config.ts"
      via: "LOCALES/DEFAULT_LOCALE/LOCALE_COOKIE imports"
      pattern: "from './src/lib/i18n/config'"
    - from: "apps/petabase/src/lib/i18n/locale.ts"
      to: "apps/petabase/src/lib/i18n/config.ts"
      via: "normalizeLocale delegation"
      pattern: "normalizeLocale"
    - from: "apps/petabase/src/lib/env.ts"
      to: "apps/petabase/src/lib/i18n/config.ts"
      via: "default locale validation against canonical constants"
      pattern: "LOCALES"
---

<objective>
Lock the core locale/config contracts for `apps/petabase` so every downstream route, hook, and shell component can rely on one canonical source of truth.

Purpose: Ensure Phase 01 foundation is deterministic before UI composition work starts.
Output: Stable locale typings + middleware routing + fail-fast env contract.
</objective>

<execution_context>
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/nrgsxjb/.openclaw/workspace-kogoro/haelabs-monorepo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@apps/petabase/src/lib/i18n/config.ts
@apps/petabase/src/lib/i18n/locale.ts
@apps/petabase/src/types/i18n.ts
@apps/petabase/src/lib/env.ts
@apps/petabase/middleware.ts

<interfaces>
From `apps/petabase/src/lib/i18n/config.ts`:
```ts
export const LOCALES = ['th', 'en'] as const;
export const DEFAULT_LOCALE = 'th';
export const LOCALE_COOKIE = 'petabase.locale';
export type AppLocale = (typeof LOCALES)[number];
export function isLocale(value: string): value is AppLocale;
export function normalizeLocale(value: string): AppLocale;
```

From `apps/petabase/src/types/i18n.ts`:
```ts
export type MessageCatalog = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize locale and message contracts used across petabase</name>
  <files>apps/petabase/src/types/i18n.ts, apps/petabase/src/lib/i18n/config.ts, apps/petabase/src/lib/i18n/locale.ts</files>
  <read_first>apps/petabase/src/types/i18n.ts, apps/petabase/src/lib/i18n/config.ts, apps/petabase/src/lib/i18n/locale.ts</read_first>
  <action>Ensure locale contracts are fully canonical for this phase scope: keep supported locales exactly `th` and `en`, keep default locale `th`, keep cookie key `petabase.locale`, and ensure normalization helpers always return `AppLocale`. Keep message catalog typing explicit (no `any`) so route/layout components can safely consume dictionaries.</action>
  <verify>
    <automated>pnpm exec nx typecheck petabase</automated>
  </verify>
  <acceptance_criteria>
    - `config.ts` exports `LOCALES`, `DEFAULT_LOCALE`, `LOCALE_COOKIE`, `AppLocale`, `isLocale`, and `normalizeLocale`.
    - `LOCALES` contains exactly two entries: `th`, `en`.
    - `types/i18n.ts` exports concrete catalog types with no `any` usage.
  </acceptance_criteria>
  <done>Locale and dictionary contracts are single-source and compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Harden middleware locale negotiation and auth-guard toggle boundaries</name>
  <files>apps/petabase/middleware.ts, apps/petabase/src/lib/env.ts</files>
  <read_first>apps/petabase/middleware.ts, apps/petabase/src/lib/env.ts, apps/petabase/src/lib/auth/protection.ts</read_first>
  <action>Implement deterministic locale negotiation order (cookie → Accept-Language → default), redirect non-localized paths to `/{locale}` preserving remaining path segments, normalize invalid two-letter locale segments, and persist locale cookie on redirect/next responses. Keep coarse auth guard optional via `NEXT_PUBLIC_ENABLE_AUTH_GUARD` so protected `/dashboard` and `/admin` paths can be gated without blocking public auth routes. In `env.ts`, validate required public env values with zod and fail startup on invalid values (do not silently default invalid configuration).</action>
  <verify>
    <automated>pnpm exec nx lint petabase && pnpm exec nx typecheck petabase</automated>
  </verify>
  <acceptance_criteria>
    - `middleware.ts` includes cookie-first and header-second locale resolution.
    - `middleware.ts` matcher excludes `_next`, `api`, and static assets.
    - `env.ts` exports a parsed/validated object and throws on invalid env.
    - Guard toggle uses `NEXT_PUBLIC_ENABLE_AUTH_GUARD` and leaves sign-in path accessible.
  </acceptance_criteria>
  <done>Locale redirect + env validation behavior is deterministic and reusable by all downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → middleware | Untrusted URL, cookie, and Accept-Language inputs enter routing logic |
| process env → env parser | Untrusted deployment configuration enters runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `middleware.ts` locale parsing | mitigate | Validate locale strictly with `isLocale`, normalize unknown/invalid values to canonical defaults |
| T-01-02 | S | auth guard cookie check | mitigate | Treat session cookie as existence-only coarse guard in this phase, no trust in user claims |
| T-01-03 | D | middleware redirect loop risk | mitigate | Exclude static/internal paths with matcher and guard conditional redirects |
| T-01-04 | I | env leakage via startup errors | mitigate | Fail fast with bounded validation errors; do not print secrets |
</threat_model>

<verification>
- `pnpm exec nx lint petabase`
- `pnpm exec nx typecheck petabase`
- `pnpm exec nx build petabase`
</verification>

<success_criteria>
- Locale/env foundation is canonical and shared consumers import it directly (no duplicate locale constants).
- Middleware always converges to locale-prefixed routing with stable cookie behavior.
- `PB-01` and `PB-02` are fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-initialize-petabase-next-js-app-foundation-structure-locale-/01-01-SUMMARY.md`
</output>
