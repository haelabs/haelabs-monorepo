---
phase: 02-api-contracts-and-health-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [R20, R21, R22]
user_setup: []

must_haves:
  truths:
    - "`nx build api` succeeds without errors"
    - "TypeScript compilation passes with strict mode"
    - "Linting passes without errors"
    - "Documentation covers all API foundation conventions"
    - "Health endpoint is deployment-ready for Railway"
    - "All environment variables are documented"
  artifacts:
    - path: "dist/apps/api"
      provides: "Production build output"
      exists: "after build"
    - path: "docs/api-foundation.md"
      provides: "API foundation documentation"
      min_lines: 200
  key_links:
    - from: "nx build api"
      to: "dist/apps/api"
      via: "compilation"
      pattern: "tsc compilation succeeds"
    - from: "docs/api-foundation.md"
      to: "Phase 1 implementation"
      via: "documentation"
      pattern: "all conventions documented"
---

<objective>
Verify build succeeds, documentation is complete, and API is deployment-ready for Railway health checks.

Purpose: Ensure all Phase 1 implementation is production-ready before adding domain modules in future phases.
Output: Verified build, complete documentation, deployment-ready health endpoint.
</objective>

<execution_context>
@/Users/nrgsxjb/.openclaw/workspace-akai/haelabs-monorepo/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/nrgsxjb/.openclaw/workspace-akai/haelabs-monorepo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 2 Context
@.planning/phases/02-api-contracts-and-health-readiness/02-CONTEXT.md

# Phase 1 Implementation Patterns
@.planning/phases/01/01-01-SUMMARY.md
@.planning/phases/01/01-02-SUMMARY.md

# API Foundation Documentation
@docs/api-foundation.md

# Phase 1 Source Files (for verification context)
@apps/api/src/main.ts
@apps/api/src/app.module.ts
@apps/api/src/common/filters/http-exception.filter.ts
@apps/api/src/common/interceptors/response-envelope.interceptor.ts
@apps/api/src/health/health.controller.ts
@apps/api/src/health/health.service.ts
@apps/api/src/config/env.validation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build and linting pass</name>
  <files>apps/api/package.json, apps/api/project.json, apps/api/tsconfig.json</files>
  <read_first>
    - apps/api/package.json (check build and lint scripts)
    - apps/api/project.json (verify Nx target configuration)
    - apps/api/tsconfig.json (confirm strict mode enabled)
  </read_first>
  <action>
    Verify that the API can be built successfully and all quality gates pass:

    1. Run `nx build api` and verify it completes without errors:
       - Build output should exist at `dist/apps/api`
       - TypeScript compilation should pass with strict mode
       - No compilation errors or warnings

    2. Run `pnpm nx lint api` and verify it passes:
       - All ESLint rules should pass
       - No linting errors or warnings
       - Test files should be included in linting (tsconfig.lint.json)

    3. Run `pnpm --filter @haelabs/api run test` and verify all tests pass:
       - All 12 contract tests should pass (from Phase 1)
       - No test failures or skipped tests
       - Coverage should be adequate for verification

    If any of these steps fail, investigate and fix the issue before proceeding.
  </action>
  <verify>
    <automated>nx build api && pnpm nx lint api && pnpm --filter @haelabs/api run test</automated>
  </verify>
  <done>
    - `nx build api` succeeds with output at `dist/apps/api`
    - `pnpm nx lint api` passes with no errors
    - All 12 contract tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify documentation completeness</name>
  <files>docs/api-foundation.md, apps/api/src/main.ts, apps/api/src/health/health.service.ts</files>
  <read_first>
    - docs/api-foundation.md (review existing documentation)
    - .planning/REQUIREMENTS.md (check R21 requirements)
    - apps/api/src/main.ts (verify routing/versioning conventions match docs)
    - apps/api/src/health/health.service.ts (verify health payload matches docs)
  </read_first>
  <action>
    Verify that the API foundation documentation covers all required conventions and extension points:

    1. Check `docs/api-foundation.md` covers:
       - ✓ Routing and versioning conventions (`/api/v1` base)
       - ✓ Configuration pattern and environment variables
       - ✓ Response envelope structure (success and error)
       - ✓ Health endpoint contract (payload structure)
       - ✓ Logging conventions (structured logs, request correlation)
       - ✓ Validation pipeline (ValidationPipe configuration)
       - ✓ CORS configuration
       - ✓ Module structure and reserved domain modules
       - ✓ Extension points for future modules
       - ✓ Onboarding path for new domain modules

    2. Verify documentation matches implementation:
       - Route base in docs matches `getRouteBase()` in main.ts
       - Health payload in docs matches HealthService.getHealth() output
       - Response envelope examples match ResponseEnvelopeInterceptor behavior
       - Error envelope examples match HttpExceptionFilter behavior
       - Env variables documented match env.validation.ts schema

    3. Check all required environment variables are documented:
       - NODE_ENV (development/test/production)
       - APP_NAME (service name)
       - API_VERSION (API version string)
       - PORT (server port)
       - LOG_LEVEL (logging level)
       - CORS_ORIGINS (comma-separated allowlist)

    If any documentation is incomplete or inaccurate, update docs/api-foundation.md.
  </action>
  <verify>
    <automated>grep -q "Routing and versioning" docs/api-foundation.md && grep -q "Response envelope" docs/api-foundation.md && grep -q "Health endpoint" docs/api-foundation.md && grep -q "Configuration pattern" docs/api-foundation.md && grep -q "Logging" docs/api-foundation.md && grep -q "Validation" docs/api-foundation.md && grep -q "CORS" docs/api-foundation.md && grep -q "Extension points" docs/api-foundation.md</automated>
  </verify>
  <done>
    - All API foundation conventions documented in docs/api-foundation.md
    - Documentation matches implementation (routing, health payload, envelopes)
    - All environment variables documented with types and descriptions
    - Extension points for future modules are clear
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deployment readiness and health endpoint</name>
  <files>apps/api/src/main.ts, apps/api/src/health/health.controller.ts</files>
  <read_first>
    - apps/api/src/main.ts (verify bootstrap configuration)
    - apps/api/src/health/health.controller.ts (verify health endpoint routing)
    - apps/api/src/health/health.service.ts (verify health payload structure)
  </read_first>
  <action>
    Verify that the API is deployment-ready for Railway health checks:

    1. Start the API server:
       ```bash
       nx serve api
       ```

    2. Test the health endpoint:
       ```bash
       curl http://localhost:3000/api/v1/health
       ```

    3. Verify health response structure:
       ```json
       {
         "status": "ok",
         "service": "haelabs-api",
         "version": "1",
         "database": {
           "ready": false,
           "status": "not_configured"
         }
       }
       ```

    4. Verify health endpoint is publicly accessible (no auth required):
       - Should return 200 status code
       - Should not require authentication headers
       - Should be compatible with Railway health checks

    5. Verify error handling works correctly:
       ```bash
       # Test validation error (invalid route)
       curl http://localhost:3000/api/v1/invalid
       ```
       Should return standardized error envelope with 400 status.

    6. Verify CORS configuration:
       - Check that CORS is configured with env-driven allowlist
       - Test with different origins if CORS_ORIGINS is set

    7. Verify logging output:
       - Startup logs should include: event, port, base, env
       - Request logs should include correlation ID

    8. Verify env validation works:
       - Stop the server
       - Try starting with invalid CORS_ORIGIN (e.g., "not-a-url")
       - Should fail fast with clear error message

    Document any issues found and fix them before marking this task complete.
  </action>
  <verify>
    <automated>MISSING — Human verification required for deployment readiness</automated>
  </verify>
  <done>
    - `nx serve api` starts successfully with structured startup logs
    - `GET /api/v1/health` returns 200 with correct payload structure
    - Health endpoint is publicly accessible and compatible with Railway checks
    - Error handling returns standardized error envelopes
    - CORS configuration works as documented
    - Request logging includes correlation IDs
    - Env validation fails fast with clear error messages
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input crosses here (requests, headers) |
| Env → API | Runtime configuration crosses here (CORS_ORIGINS, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | CORS configuration | accept | CORS origins validated at startup (Phase 1). This verification task confirms it works correctly. |
| T-02-02 | S | Health endpoint | accept | Public endpoint is acceptable for health checks. No sensitive data exposed. |
| T-02-03 | I | Request logging | accept | Logs are structured but no sensitive data in request path. Phase 1 sanitization removes sensitive fields. |
| T-02-04 | D | Error messages | accept | Phase 1 sanitization removes target/value from validation errors. This verification confirms it's working. |
</threat_model>

<verification>
## Overall Phase Verification

1. **Build Verification**: `nx build api` succeeds with TypeScript strict mode
2. **Linting Verification**: `pnpm nx lint api` passes with no errors
3. **Test Verification**: All 12 contract tests pass
4. **Documentation Verification**: All conventions documented and match implementation
5. **Deployment Verification**: Health endpoint works and is Railway-compatible
6. **Error Handling Verification**: Standardized error envelopes returned
7. **Logging Verification**: Structured logs with correlation IDs
8. **Env Validation Verification**: Fail-fast behavior works correctly

## Requirements Coverage

- **R20**: `nx build api` succeeds → Verified in Task 1
- **R21**: Documentation complete → Verified in Task 2
- **R22**: Deployment-ready for Railway → Verified in Task 3

## Success Criteria

All verification steps pass with no blocking issues. API is production-ready for local development, CI, and Railway deployment.
</verification>

<success_criteria>
- `nx build api` succeeds with output at `dist/apps/api`
- `pnpm nx lint api` passes with no errors
- All 12 contract tests pass
- `docs/api-foundation.md` documents all API foundation conventions
- Health endpoint returns 200 with correct payload structure
- Health endpoint is publicly accessible and Railway-compatible
- Error handling returns standardized error envelopes
- Request logging includes correlation IDs
- Env validation fails fast with clear error messages
- All remaining requirements (R20, R21, R22) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-contracts-and-health-readiness/02-01-SUMMARY.md`
</output>
