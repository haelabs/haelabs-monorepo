---
phase: 03-documentation-and-handoff-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api-foundation.md
  - README.md
  - docs/monorepo-foundation.md
autonomous: true
requirements: [R21, R22]
user_setup: []

must_haves:
  truths:
    - "Contributors can find one canonical backend foundation reference from both README and monorepo docs."
    - "API foundation docs explicitly define module extension points, ownership boundaries, and onboarding flow for apps/api/src/modules/*."
    - "The documented backend contracts remain explicit and unchanged: /api/v1 routing, config fail-fast behavior, success/error envelopes, request correlation, and public health endpoint behavior."
    - "Contributors have a concrete verification path with automated commands plus explicit carry-forward runtime checks from Phase 2 verification."
  artifacts:
    - path: "docs/api-foundation.md"
      provides: "Canonical backend foundation + handoff guidance"
      contains: "Canonical Documentation Ownership"
    - path: "README.md"
      provides: "Root contributor pointer to canonical backend doc"
      contains: "docs/api-foundation.md"
    - path: "docs/monorepo-foundation.md"
      provides: "Monorepo docs pointer to canonical backend doc"
      contains: "docs/api-foundation.md"
  key_links:
    - from: "README.md"
      to: "docs/api-foundation.md"
      via: "canonical backend docs link"
      pattern: "api-foundation.md"
    - from: "docs/monorepo-foundation.md"
      to: "docs/api-foundation.md"
      via: "backend conventions link"
      pattern: "api-foundation.md"
    - from: "docs/api-foundation.md"
      to: ".planning/phases/02-api-contracts-and-health-readiness/02-VERIFICATION.md"
      via: "carry-forward runtime verification notes"
      pattern: "Live health endpoint response|Public accessibility"
---

<objective>
Harden Phase 3 documentation handoff so future contributors can extend `apps/api` without changing backend foundation conventions.

Purpose: Complete documentation fidelity and discoverability for canonical API foundation guidance while preserving Phase 1-2 contracts.
Output: Updated canonical backend doc plus explicit cross-links from repository entry docs.
</objective>

<execution_context>
@/Users/nrgsxjb/.openclaw/workspace-akai/haelabs-monorepo/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/nrgsxjb/.openclaw/workspace-akai/haelabs-monorepo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-documentation-and-handoff-completeness/03-CONTEXT.md
@.planning/phases/02-api-contracts-and-health-readiness/02-VERIFICATION.md

@docs/api-foundation.md
@docs/monorepo-foundation.md
@README.md

@apps/api/src/main.ts
@apps/api/src/app.module.ts
@apps/api/src/config/env.validation.ts
@apps/api/src/common/filters/http-exception.filter.ts
@apps/api/src/common/interceptors/response-envelope.interceptor.ts
@apps/api/src/health/health.controller.ts
@apps/api/src/health/health.service.ts
@apps/api/src/modules/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden canonical API foundation handoff content</name>
  <files>docs/api-foundation.md</files>
  <read_first>
    - docs/api-foundation.md
    - .planning/phases/03-documentation-and-handoff-completeness/03-CONTEXT.md
    - .planning/phases/02-api-contracts-and-health-readiness/02-VERIFICATION.md
    - apps/api/src/main.ts
    - apps/api/src/config/env.validation.ts
    - apps/api/src/common/filters/http-exception.filter.ts
    - apps/api/src/common/interceptors/response-envelope.interceptor.ts
    - apps/api/src/health/health.service.ts
  </read_first>
  <action>
    Update `docs/api-foundation.md` as the single canonical backend foundation reference (per D-01, D-03) with explicit handoff sections that preserve current implementation truth (per D-05):

    1. Add/refresh a section with heading `## Canonical Documentation Ownership` stating this file is the canonical API foundation reference and that other docs should link to it rather than duplicate backend conventions.
    2. Add/refresh a section with heading `## Module Extension Ownership Boundaries` that explicitly defines:
       - where future modules live (`apps/api/src/modules/*`)
       - what belongs in modules vs `common/`, `config/`, `infrastructure/`, `shared/`
       - that Phase 3 is docs-only and does not change runtime contracts.
    3. Ensure routing/versioning, fail-fast env validation, success/error envelope, request correlation, and public health endpoint sections remain concrete and implementation-aligned (`/api/v1`, `HttpExceptionFilter`, `ResponseEnvelopeInterceptor`, `x-request-id`, DB-readiness stub shape) per D-05.
  </action>
  <acceptance_criteria>
    - `docs/api-foundation.md` contains heading `## Canonical Documentation Ownership`
    - `docs/api-foundation.md` contains heading `## Module Extension Ownership Boundaries`
    - `docs/api-foundation.md` includes literal `apps/api/src/modules/*`
    - `docs/api-foundation.md` includes literal `/api/v1`
    - `docs/api-foundation.md` includes literals `HttpExceptionFilter`, `ResponseEnvelopeInterceptor`, and `x-request-id`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Canonical Documentation Ownership|Module Extension Ownership Boundaries|apps/api/src/modules/\*|/api/v1|HttpExceptionFilter|ResponseEnvelopeInterceptor|x-request-id" docs/api-foundation.md</automated>
  </verify>
  <done>
    Canonical handoff content is explicit, non-duplicative, and preserves existing backend contract details without runtime scope creep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add canonical cross-links from repository entry docs</name>
  <files>README.md, docs/monorepo-foundation.md</files>
  <read_first>
    - README.md
    - docs/monorepo-foundation.md
    - docs/api-foundation.md
    - .planning/phases/03-documentation-and-handoff-completeness/03-CONTEXT.md
  </read_first>
  <action>
    Update `README.md` and `docs/monorepo-foundation.md` to include concise contributor pointers to `docs/api-foundation.md` as the canonical backend foundation doc (per D-02, D-01).

    Requirements for both files:
    - Add an explicit sentence that `docs/api-foundation.md` is canonical for backend foundation conventions.
    - Keep pointers short (1-2 lines) and avoid duplicating routing/config/envelope details in these entry docs (per D-01).
    - Preserve existing references to other docs (`docs/petabase-foundation.md`, conventions docs) so navigation remains complete.
  </action>
  <acceptance_criteria>
    - `README.md` contains `docs/api-foundation.md` and `canonical`
    - `docs/monorepo-foundation.md` contains `docs/api-foundation.md` and `canonical`
    - Neither file introduces detailed backend contract sections that duplicate `docs/api-foundation.md`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "api-foundation\.md|canonical" README.md docs/monorepo-foundation.md</automated>
  </verify>
  <done>
    Contributors entering from root docs are routed directly to the canonical backend foundation reference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Embed contributor verification path with carry-forward runtime checks</name>
  <files>docs/api-foundation.md</files>
  <read_first>
    - docs/api-foundation.md
    - .planning/phases/02-api-contracts-and-health-readiness/02-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    In `docs/api-foundation.md`, add or refresh a handoff subsection named `## Contributor Verification Path` (or equivalent explicit heading) that includes:

    1. Automated verification commands (per D-06):
       - `pnpm nx build api`
       - `pnpm nx lint api`
       - `pnpm --filter @haelabs/api run test`
    2. Carry-forward runtime checks from Phase 2 verification (per D-06, R22):
       - live `GET /api/v1/health` over HTTP returns 200 with success envelope
       - health endpoint is publicly accessible without auth headers.
    3. A reference to `.planning/phases/02-api-contracts-and-health-readiness/02-VERIFICATION.md` as historical evidence for what remains runtime/manual.

    Keep this section procedural and handoff-focused; do not change runtime behavior or introduce new endpoint requirements.
  </action>
  <acceptance_criteria>
    - `docs/api-foundation.md` contains `pnpm nx build api`, `pnpm nx lint api`, and `pnpm --filter @haelabs/api run test`
    - `docs/api-foundation.md` contains text for both runtime checks: live health response and no-auth public access
    - `docs/api-foundation.md` references `02-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>pnpm nx build api && pnpm nx lint api && pnpm --filter @haelabs/api run test && rg -n "Contributor Verification Path|pnpm nx build api|pnpm nx lint api|pnpm --filter @haelabs/api run test|02-VERIFICATION\.md|without auth|public" docs/api-foundation.md</automated>
  </verify>
  <done>
    Documentation includes a concrete execution-ready verification path plus explicit carry-forward runtime checks required for deployment confidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor → Documentation | Contributors depend on docs as operational truth; incorrect guidance can cause unsafe or incompatible changes. |
| Entry docs → Canonical backend doc | Broken or missing links can cause drift and fragmented conventions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `docs/api-foundation.md` ownership guidance | mitigate | Declare canonical ownership section and enforce cross-links from README/monorepo docs (Tasks 1-2). |
| T-03-02 | R | Contributor handoff verification guidance | mitigate | Add explicit command-based verification path + runtime checks with source citation to `02-VERIFICATION.md` (Task 3). |
| T-03-03 | I | Documentation examples and env/config details | accept | Existing docs describe schema/commands only; no secrets are added and phase forbids `.env` values per AGENTS.md. |
| T-03-04 | D | Onboarding flow for future module contributors | mitigate | Explicit module ownership boundaries + onboarding flow at `apps/api/src/modules/*` reduce ambiguity-induced delays (Task 1). |
</threat_model>

<verification>
Run end-to-end documentation and quality checks:

1. `rg -n "Canonical Documentation Ownership|Module Extension Ownership Boundaries|apps/api/src/modules/\*|/api/v1|HttpExceptionFilter|ResponseEnvelopeInterceptor|x-request-id" docs/api-foundation.md`
2. `rg -n "api-foundation\.md|canonical" README.md docs/monorepo-foundation.md`
3. `pnpm nx build api && pnpm nx lint api && pnpm --filter @haelabs/api run test`
4. `rg -n "Contributor Verification Path|02-VERIFICATION\.md|without auth|public|GET /api/v1/health" docs/api-foundation.md`
</verification>

<success_criteria>
- `docs/api-foundation.md` is explicitly identified as canonical and includes ownership boundaries + module onboarding guidance.
- `README.md` and `docs/monorepo-foundation.md` both point contributors to `docs/api-foundation.md` as canonical backend reference.
- API foundation conventions remain documented as implementation truths (routing/versioning, config fail-fast, envelopes, logging/request correlation, public health endpoint contract).
- Contributor verification path includes automated commands and explicit carry-forward runtime checks from Phase 2 verification evidence.
- Requirements R21 and R22 are fully covered by this plan without introducing runtime scope changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-and-handoff-completeness/03-01-SUMMARY.md`
</output>
